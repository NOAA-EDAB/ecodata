# StatLM Stat protto based on ecodata StatGLS Need to pass arguments

nBootSamples, pVal significance, number datapoints

## Usage

``` r
StatLM
```

## Format

An object of class `StatLM` (inherits from `Stat`, `ggproto`, `gg`) of
length 4.
